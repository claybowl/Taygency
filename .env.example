# ===========================================
# Vibe Planning - Environment Variables
# Single-user prototype with GitHub storage
# ===========================================

# ------------------------------------------
# GitHub Storage (Required)
# ------------------------------------------
# Create a fine-grained personal access token at:
# https://github.com/settings/tokens?type=beta
# Required permissions: Contents (read/write)
GITHUB_TOKEN=ghp_your_personal_access_token
GITHUB_OWNER=Claybowl
GITHUB_REPO=Taygency
GITHUB_DATA_PATH=task-data

# ------------------------------------------
# Anthropic API (Required)
# ------------------------------------------
ANTHROPIC_API_KEY=sk-ant-xxx

# ------------------------------------------
# SendGrid (Required for email integration)
# ------------------------------------------
SENDGRID_API_KEY=SG.xxx
SENDGRID_FROM_EMAIL=tasks@vibeplan.com

# ------------------------------------------
# VAPI (Required for SMS/Voice)
# ------------------------------------------
VAPI_API_KEY=xxx
VAPI_PHONE_NUMBER=+15551234567

# ------------------------------------------
# Upstash Redis (Rate Limiting)
# Optional - app works without, but no rate limiting
# ------------------------------------------
UPSTASH_REDIS_URL=xxx
UPSTASH_REDIS_TOKEN=xxx
